apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: teslamate
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`teslamate.ryanb.tv`)
      kind: Rule
      services:
        - name: teslamate
          kind: Service
          port: http
  tls: {}
